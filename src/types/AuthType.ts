export type loginData = {
  email: string;
  password: string;
};
export type regisData = {
  email: string;
  password: string;
  fullname: string;
};
export type recruiterData = {
  email: string;
  companyName: string;
  phoneNumber: string;
  address: string;
  websiteUrl: string;
};
